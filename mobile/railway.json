{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node -e \"const v=process.env.EXPO_PUBLIC_GA4_MEASUREMENT_ID; console.log('[diag] GA4_VAR_PRESENT=' + (v ? ('yes len=' + v.length) : 'no'))\" && CI=false npm run build:web"
  },
  "deploy": {
    "startCommand": "npm run start:web"
  }
}
